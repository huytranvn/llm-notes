import SwiftUI

public struct ChatPaneView: View {
    public init() {}
    public var body: some View {
        Text("Chat — see openspec/changes/add-llm-chat")
            .frame(minWidth: 300)
    }
}
