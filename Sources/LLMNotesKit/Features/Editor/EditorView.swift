import SwiftUI

public struct EditorView: View {
    public init() {}
    public var body: some View {
        Text("Editor — see openspec/changes/add-markdown-editor")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
